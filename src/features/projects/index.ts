export { ProjectDetailView } from './ProjectDetailView';
export { CreateProjectView } from './CreateProjectView';

export const ProjectsListPage = () => {
  // The projects list lives inside the Dashboard (tab=PROJECTS).
  // This export is a convenience re-export for the router.
  // Import Dashboard from features/dashboard instead.
};
